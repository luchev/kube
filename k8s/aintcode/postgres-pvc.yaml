# Persistent volume for PostgreSQL data.
# For local kind the default storage class provisions a hostPath volume.
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: postgres-data
  namespace: aintcode
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
